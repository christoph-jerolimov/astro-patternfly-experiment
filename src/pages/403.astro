---
import { PageSection } from '@patternfly/react-core';

import AppLayout from '../components/AppLayout';
import ErrorState from '../components/ErrorState';
import Layout from '../layouts/Layout.astro';
---

<Layout title="Error 403 — Astro + PatternFly" description="A PatternFly 403 error page.">
  <!--
    Error pages keep the masthead and sidebar. Losing the navigation is the
    last thing someone who is already lost needs.
  -->
  <AppLayout client:load activeItem="403">
    <PageSection isFilled>
      <ErrorState code="403" />
    </PageSection>
  </AppLayout>
</Layout>
